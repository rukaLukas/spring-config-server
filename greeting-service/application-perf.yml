# server:
#   port: 8080
#   servlet:
#     context-path: /

spring:
  application:
    name: greeting-service
management:
  server:
    port: 8888
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"

greeting-service:
    greeting: "modified 2 perf"
    default-value: "Anonymous"